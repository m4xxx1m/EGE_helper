<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="ru.maximivanov.ege_helper">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_launcher_round" android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".ChoseSubjectsActivity">
        </activity>
        <activity android:name=".LogInActivity">
        </activity>
        <activity android:name=".SignUpActivity">
        </activity>
        <activity android:name=".MainPageActivity"
            android:label="@string/home">
        </activity>
        <activity android:name=".TestsPageActivity"
            android:label="@string/test">
        </activity>
        <activity android:name=".TheoryPageActivity"
            android:label="@string/theory">
        </activity>
        <activity android:name=".StatisticPageActivity"
            android:label="@string/statistic">
        </activity>
        <activity android:name=".SettingsPageActivity"
            android:label="@string/settings">
        </activity>
        <activity android:name=".TestActivity"
            android:theme="@style/TestTheme">
        </activity>
        <activity android:name=".StartPageActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>